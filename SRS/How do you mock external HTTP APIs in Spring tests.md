<!--
reps: 0
priority: 0
-->
#Java/Spring/Framework/Testing #Testing/Mocking #SRS #New

> [!warning] Untrusted draft
> Copied from an external question dump. Not checked against official documentation. Do not treat this as a review answer.

Dumps: `MockRestServiceServer`, **WireMock**, or `@MockBean` on `RestTemplate` / `WebClient` beans.

Use this so `@SpringBootTest` does not call the real vendor.

> [!warning] Unverified traps from the dump
> - WireMock is a separate leaf (#Java/Testing/WireMock); this card is the Spring-test question.
