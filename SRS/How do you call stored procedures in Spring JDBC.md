<!--
reps: 0
priority: 0
-->
#Java/Spring/Framework/DataAccess #SRS #New

> [!warning] Untrusted draft
> Copied from an external question dump. Not checked against official documentation. Do not treat this as a review answer.

Climb: three ways — `SimpleJdbcCall`, `JdbcTemplate.execute()`, or `JdbcTemplate.call()`.

Another Q&A names `org.springframework.jdbc.object.StoredProcedure` as a class that encapsulates calling a stored procedure, including input/output parameters and exception conditions.

> [!warning] Unverified traps from the dump
> - Three names in two dumps is already a collision: `call` / `execute` / `SimpleJdbcCall` / `StoredProcedure`. Treat them as dump-listed options, not a ranked official menu.
> - Plain JDBC stored-procedure cards in this vault are a different leaf (`#Java/JDBC`).

