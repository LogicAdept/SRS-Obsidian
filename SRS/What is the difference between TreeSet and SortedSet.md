<!--
reps: 0
priority: 0
-->
#Java/Collections/Set/TreeSet #SRS #New

> [!warning] Untrusted draft
> Copied from an external question dump. Not checked against official documentation. Do not treat this as a review answer.

Dump: **`SortedSet` is the interface. `TreeSet` is the implementation.** That is the whole answer in that compilation.

> [!warning] Unverified traps from the dump
> - Later JDKs also have `NavigableSet` (which `TreeSet` implements). This dump only names `SortedSet`.
> - `ConcurrentSkipListSet` is another sorted set in concurrent dumps, not mentioned here.
