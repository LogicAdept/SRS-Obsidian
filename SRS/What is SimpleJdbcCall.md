<!--
reps: 0
priority: 0
-->
#Java/Spring/Framework/DataAccess #SRS #New

> [!warning] Untrusted draft
> Copied from an external question dump. Not checked against official documentation. Do not treat this as a review answer.

Java Code Geeks lists `SimpleJdbcCall` as one Spring JDBC approach. Climb: one of three ways to call a stored procedure in Spring JDBC is `SimpleJdbcCall` (the others are `JdbcTemplate.execute` and `JdbcTemplate.call`).

> [!warning] Unverified traps from the dump
> - Dumps do not give a full `SimpleJdbcCall` fluent example in the answers used here.
> - There is also a `StoredProcedure` class on another interview list — do not assume one API name.

