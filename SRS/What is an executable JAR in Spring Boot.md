<!--
reps: 0
priority: 0
-->
#Java/Spring/Boot/Build #SRS #New

> [!warning] Untrusted draft
> Copied from an external question dump. Not checked against official documentation. Do not treat this as a review answer.

Boot packages a fat/executable JAR that embeds the app plus an embedded servlet container. Run with `java -jar app.jar` — no separate Tomcat install.

> [!warning] Unverified traps from the dump
> - A plain Maven jar without the Spring Boot plugin is not that executable layout.
