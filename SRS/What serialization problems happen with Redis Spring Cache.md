<!--
reps: 0
priority: 0
-->
#Java/Spring/Framework/Cache #Java/Spring/Data/Redis #SRS #New

> [!warning] Untrusted draft
> Copied from an external question dump. Not checked against official documentation. Do not treat this as a review answer.

Dump list: Hibernate lazy proxies serialize as proxies — use DTOs; Lombok `@Builder` without a no-arg constructor breaks Jackson; `LocalDateTime` needs `JavaTimeModule`; polymorphic types need typing; JDK serialization breaks when fields change — prefer JSON.

> [!warning] Unverified traps from the dump
> - These are Redis/Jackson issues on top of the Spring Cache abstraction, not `@Cacheable` SpEL bugs.

