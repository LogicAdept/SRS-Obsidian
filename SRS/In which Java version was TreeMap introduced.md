<!--
reps: 0
priority: 0
-->
#Java/Collections/Map/TreeMap #SRS #New

> [!warning] Untrusted draft
> Copied from an external question dump. Not checked against official documentation. Do not treat this as a review answer.

A comparison dump: **`TreeMap` arrived in JDK 2.0** (Java 2 / Collections Framework). **`ConcurrentSkipListMap`** in the same table is **JDK 6.0**.

`NavigableMap` (extra floor/ceiling/poll methods) is called out separately as JDK 6 on `TreeMap`.

> [!warning] Unverified traps from the dump
> - Do not collapse “TreeMap exists” with “NavigableMap exists”: dumps put TreeMap in Java 2 and NavigableMap in Java 6.
